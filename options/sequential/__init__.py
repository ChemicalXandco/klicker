from options.sequential import (
scroll,
mouseDown,
mouseUp,
wait
)

optDict = {'scroll': scroll,
           'mouseDown': mouseDown,
           'mouseUp': mouseUp,
           'wait': wait
           }

optList = list(optDict.keys())
