from options.nonsequential import (
holdClick,
timer,
holdKey,
doOnce,
onStart,
onEnd
)

optDict = {'holdClick': holdClick,
           'timer': timer,
           'holdKey': holdKey,
           'doOnce': doOnce,
           'onStart': onStart,
           'onEnd': onEnd
           }

optList = list(optDict.keys())
