from options.nonsequential import (
holdClick,
timer,
holdKey,
doOnce
)

optDict = {'holdClick': holdClick,
           'timer': timer,
           'holdKey': holdKey,
           'doOnce': doOnce
           }

optList = list(optDict.keys())
